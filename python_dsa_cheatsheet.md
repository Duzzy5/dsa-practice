# Read integer
n = int(input())

# Read list
arr = list(map(int, input().split()))

# Sum
sum(arr)

# Length
len(arr)

# First element
arr[0]

# Last element
arr[-1]

# Add element
arr.append(x)

# Check substring
if "++" in s:
